#include <stdio.h>
#include <stdlib.h>

int main()
{

    do
    {

printf("Sexo:")
scanf("%c",&sexo);

system(cls || clear);

printf("Idade:")
scanf("%d",&idade)
        
        printf("Digite 1 para Adicionar Familia");
        printf("Digite 2 para sair e exibir os resultados");
        scanf("%d", &resposta)

    } while (resposta == 1);

    return 0;
}